<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>
        {{ siteTitle }}
        {{#if doc.title }}
         - {{ doc.title }}
         {{/if }}
    </title>
</head>
<body>
    <header>
        {{> menu }}
    </header>